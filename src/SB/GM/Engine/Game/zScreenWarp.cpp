// GENERATED by tools/transplant.py from a matched twin.
// Do not hand-edit: re-emit, or take the file over and remove this
// banner -- written_vs_generated.py splits on it.
//
// ScreenWarp, transplanted from
// Create__Q24Sext13zSpinnerAssetFPQ25World16EntityHandleBasePQ24Sext13zSpinnerAsset,
// which is matched. The shapes are identical instruction for
// instruction; the holes were 2, and every one of them is
// read from the retail image rather than guessed:
//
//   +0x020  li      r3,88
//   +0x030  li      r5,88
//
// The entity constructor is a CALL, so it is declared and never
// defined and no vtable is stored here. The class is padded to
// the size the allocation asks for, which is the only thing
// about its layout this file knows.
//
// The DWARF places this function in zScreenWarp.cpp.

enum eMemMgrTag { eMemMgrTag_ = 0x7FFFFFFF };

namespace Memory {
enum GlobalHeapEnum { GlobalHeapEnum_ = 0x7FFFFFFF };

void* AllocGlobalHeap(unsigned long size, GlobalHeapEnum heap, eMemMgrTag tag,
                      bool clear);
}  // namespace Memory

extern "C" {
void* memset(void* dst, int c, unsigned long n);
}

inline void* operator new(unsigned long, void* p) { return p; }

namespace World { class EntityHandleBase; }



namespace Sext { class ScreenWarp; }

class ScreenWarp {
public:
    ScreenWarp(World::EntityHandleBase* handle,
                   Sext::ScreenWarp* asset);

    unsigned char _pad0[0x58];
};

namespace Sext {

class ScreenWarp {
public:
    static ::ScreenWarp* Create(World::EntityHandleBase* handle,
                                ScreenWarp* asset);
};

}  // namespace Sext

ScreenWarp* Sext::ScreenWarp::Create(World::EntityHandleBase* handle,
                                     ScreenWarp* asset) {
    return new (memset(Memory::AllocGlobalHeap(
                           sizeof(::ScreenWarp), (Memory::GlobalHeapEnum)0,
                           (eMemMgrTag)16, false),
                       0, sizeof(::ScreenWarp))) ::ScreenWarp(handle, asset);
}

namespace Sext { class FXScreenWarp; }

class FXScreenWarp {
public:
    FXScreenWarp(World::EntityHandleBase* handle,
                     Sext::FXScreenWarp* asset);

    unsigned char _pad0[0x60];
};

namespace Sext {

class FXScreenWarp {
public:
    static ::FXScreenWarp* Create(World::EntityHandleBase* handle,
                                  FXScreenWarp* asset);
};

}  // namespace Sext

FXScreenWarp* Sext::FXScreenWarp::Create(World::EntityHandleBase* handle,
                                         FXScreenWarp* asset) {
    return new (memset(Memory::AllocGlobalHeap(
                           sizeof(::FXScreenWarp), (Memory::GlobalHeapEnum)0,
                           (eMemMgrTag)16, false),
                       0, sizeof(::FXScreenWarp))) ::FXScreenWarp(handle, asset);
}
