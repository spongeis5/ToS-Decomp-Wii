// GENERATED by tools/transplant.py from a matched twin.
// Do not hand-edit: re-emit, or take the file over and remove this
// banner -- written_vs_generated.py splits on it.
//
// zTriggerEntity, transplanted from
// Create__Q24Sext11xGroupAssetFPQ25World16EntityHandleBasePQ24Sext11xGroupAsset,
// which is matched. The shapes are identical instruction for
// instruction; the holes were 4, and every one of them is
// read from the retail image rather than guessed:
//
//   +0x020  li      r3,240
//   +0x030  li      r5,240
//   +0x048  bl        -> __ct__8zTriggerFPQ25World16EntityHandleBase
//   +0x050  addi    r3,r3,11640  = 806C2D78  __vt__14zTriggerEntity
//
// The DWARF places this function in zTriggerEntity.cpp.

enum eMemMgrTag { eMemMgrTag_ = 0x7FFFFFFF };

namespace Memory {
enum GlobalHeapEnum { GlobalHeapEnum_ = 0x7FFFFFFF };

void* AllocGlobalHeap(unsigned long size, GlobalHeapEnum heap, eMemMgrTag tag,
                      bool clear);
}  // namespace Memory

extern "C" {
void* memset(void* dst, int c, unsigned long n);
}

inline void* operator new(unsigned long, void* p) { return p; }

namespace World {
class EntityHandleBase;
}

class zTriggerEntity;


// The base is a CALL, so its layout is never read here. It
// is declared with the vtable pointer the derived
// constructor stores at +0 and nothing else, and the
// derived class carries the whole allocation as padding.
// That is all these bytes say about it.
class zTrigger {
public:
    zTrigger(World::EntityHandleBase* handle);

    virtual void _v0();
};

namespace Sext {

class TriggerEntityAsset {
public:
    static ::zTriggerEntity* Create(World::EntityHandleBase* handle,
                                    TriggerEntityAsset* asset);
};

}  // namespace Sext


class zTriggerEntity : public zTrigger {
public:
    zTriggerEntity(World::EntityHandleBase* handle) : zTrigger(handle) {}

    virtual void _v0();


    void Init(Sext::TriggerEntityAsset* asset);

    unsigned char _pad0[0xF0 - 0x4];
};


zTriggerEntity* Sext::TriggerEntityAsset::Create(World::EntityHandleBase* handle,
                                                 TriggerEntityAsset* asset) {
    ::zTriggerEntity* entity = new (memset(
        Memory::AllocGlobalHeap(sizeof(::zTriggerEntity), (Memory::GlobalHeapEnum)0,
                                (eMemMgrTag)16, false),
        0, sizeof(::zTriggerEntity))) ::zTriggerEntity(handle);

    entity->Init((Sext::TriggerEntityAsset*)asset);

    return entity;
}
